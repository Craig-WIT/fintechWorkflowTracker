{{> menu id="dashboard"}}

<section id= "segment" class="ui segment" style="border: 3px solid black; border-radius: 10px; background-color: #DB65C6;">
  <h2 class="ui header" style="color: white;">
    Teams Dashboard: {{user.firstname}} {{user.lastname}}
  </h2>
</section>

{{#if teams}}
  {{#each teams}}
      {{> teamDashboardSummary}}
  {{/each}}
  {{/if}}